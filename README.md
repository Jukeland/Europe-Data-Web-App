# To run the project

1) cd <absolute_path_to>\Project_thesis
2) docker-compose up --build -d

You also need to restart the app after a few seconds the very first time you run it:

3) docker restart europe_data_app

*Note: If for some reason you shutdown the container using the -v flag 
        OR if you run it again at a new machine, you should do the restart again

